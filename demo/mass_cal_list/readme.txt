Save .xlsx as .txt. Remove the first line in mass calculation table ("Axx.txt"). Make sure the position of last line locates just under the last row with numbers.

No need to remove the first row in "folderlist.txt"